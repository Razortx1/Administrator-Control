import { React } from "react";

export function URLPattern() {
    const url_aws = '' //Aqui va la url perteneciente al back end con relacion a AWS
    const url_local = 'http://localhost:8000/' //Aqui va la url perteneciente al localhost

    return url_local

}